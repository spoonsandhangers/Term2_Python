"""
Change the wording of this question so it is not OOP related
create an array containing the days of the week as a global variable
create a subroutine that iterates through the array and prints each element out
to the Python shell.

"""

#global array
daysOfWeek = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"]

def days():
	 for day in daysOfWeek:
		 print(day)

days()
